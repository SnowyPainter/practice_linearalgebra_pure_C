# Practices about Linear Alegebra with Pure C
## Setup

To create new lession,
```sh
bash tools/new_lec.sh lec03_projection
```

To Run a lession,
```
CTRL + SHIFT + P -> Select [RUN TASK] -> run:one
```